interface Props {
  topbarNavLinks: TopbarNavLink[];
  alwaysShowMenu?: boolean;
}

export interface TopbarNavLink {
  linkText: string;
  linkLocation: string;
}

export const TopbarLinks = (props: Props) => {

  return (
    <div>
    </div>
  );
};
